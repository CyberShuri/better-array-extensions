# This is a module that extends Array.prototype to maximize performance and minimize effort using arrays.

How to use:

```js
// All extensions
require('better-array-extensions')

// Destructuring
const { random, compact } = require('better-array-extensions')
```
